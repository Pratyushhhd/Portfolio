# Optional: URL of your deployed AI proxy (see api/ai.js).
# Leave empty to use the built-in offline assistant (works on GitHub Pages).
VITE_AI_API_URL=

# Server-side ONLY — set these in your hosting provider's dashboard, never commit real values.
AI_API_KEY=
AI_API_BASE=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
